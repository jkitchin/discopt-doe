"""Streamlit GUI for ``discopt doe`` campaigns.

The GUI is a thin Streamlit wrapper over the same ``do_new`` /
``do_status`` / ``do_fit`` / ``do_extend`` functions that power the
CLI. Launch it with ``discopt doe gui [WORKBOOK]``.
"""
